# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
# This 'env' section defines the environment variables available to your app.
# This is the correct way to configure secrets for production.
env:
  # The environment variables for payment gateways will now be set directly
  # on the backend using the Firebase CLI `firebase apphosting:backends:update` command.
  # This avoids potential issues with file parsing and deployment caching.
  # See your conversation with the AI assistant for the exact commands to run.
  - variable: WECHAT_API_V3_KEY
    secret: wechat-api-v3-key
